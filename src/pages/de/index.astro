---
import Base from '../../layouts/Base.astro'
import Home from '../../components/Home.astro'
const locale = 'de' as const
---
<Base locale={locale} title="Klimaanlagen in Alanya — Verkauf, Montage & Wartung | Gree Vertragshändler" path="">
  <Home locale={locale} />
</Base>
