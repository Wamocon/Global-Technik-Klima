---
import Base from '../../layouts/Base.astro'
import Home from '../../components/Home.astro'
const locale = 'en' as const
---
<Base locale={locale} title="Air Conditioning in Alanya — Sales, Installation & Service | Gree Dealer" path="">
  <Home locale={locale} />
</Base>
